
**🟪 Day 6 — Optimization in Synthesis ⚙️**

🎯 Objectives

The objective of Day 6 was to understand how different RTL coding styles influence synthesis results and how synthesis tools identify the hardware required from a Verilog description.

During this session, I explored incomplete conditional statements, case statements, partial assignments, multiplexers, demultiplexers, and an 8-bit Ripple Carry Adder. The designs were simulated using Icarus Verilog, analyzed using GTKWave, and synthesized using Yosys.

The main concepts covered were:

Incomplete IF Statement
RTL Schematic Generation
Incomplete IF-ELSE Statement
Incomplete and Complete Case Statements
Partial Case Assignment
Multiplexer and Demultiplexer
Ripple Carry Adder
Synthesis behavior and optimization

⸻

🔧 Introduction to Synthesis Optimization

Synthesis optimization is the process of converting RTL code into an efficient hardware implementation while preserving its intended functionality.

The synthesis tool analyzes the RTL description and determines the logic elements required to implement the design. The way a circuit is coded can therefore have a direct effect on the hardware generated by synthesis.

During these experiments, I observed how incomplete assignments can result in latch inference, while complete combinational descriptions allow the synthesis tool to generate more predictable logic.

The overall flow followed was:

Verilog RTL → Simulation → Waveform Analysis → Synthesis → RTL Schematic → Hardware Understanding

⸻

📌 1. Incomplete IF Statement ⚠️

An incomplete IF statement occurs when an output is assigned only for a particular condition and no assignment is provided for the remaining conditions.

For combinational logic, this can cause the output to retain its previous value. During synthesis, the tool may therefore infer a latch to provide the required storage behavior.

⚙️ Simulation Commands


iverilog -o incom_if incom_if.v incom_if_tb.v

./a.out

gtkwave incom_if.vcd

🖼️ Simulation Output

<img width="958" height="930" alt="image" src="https://github.com/user-attachments/assets/d46a4657-16f8-445f-b8d5-39c8844912cf" />

Figure 1: Simulation waveform of the incomplete IF statement.

The waveform demonstrates the behavior of the incomplete IF statement. When the specified condition is active, the output responds to the input. When the condition is not satisfied, the output retains its previous value.

This waveform helped me understand the connection between incomplete RTL assignments and memory-retention behavior.

⸻

📌 2. RTL Schematic of Incomplete IF Statement 🔍

After simulation, the same RTL design was synthesized using Yosys to understand the hardware inferred from the incomplete conditional statement.

Yosys Commands:

yosys

read_verilog incom_if.v

synth -top incom_if

show

🖼️ Synthesized Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 00 57 PM (1)" src="https://github.com/user-attachments/assets/38d3d72d-ce01-4fcc-bea1-8676e9f4a4ec" />

Figure 2: RTL schematic generated for the incomplete IF statement.

The RTL schematic generated by Yosys provides a visual representation of the hardware inferred from the Verilog description.

The schematic helps demonstrate that incomplete combinational assignments can result in storage elements rather than purely combinational logic.

This experiment made it easier to visualize how RTL code is converted into hardware during synthesis.

⸻

📌 3. RTL Simulation of Incomplete IF Statement 🧪

The simulation waveform was further examined to understand the relationship between the select signal and output behavior.

⚙️ Commands

iverilog -o incom_if incom_if.v incom_if_tb.v

./a.out

gtkwave incom_if.vcd

🖼️ Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 00 57 PM (2)" src="https://github.com/user-attachments/assets/ab88c450-5d5c-46b0-9f20-2c7cca4369c7" />

Figure 3: RTL simulation waveform of the incomplete IF design.

The waveform shows the relationship between the input, select signal, and output.

When the condition is true, the output changes according to the RTL assignment. When the condition becomes false, the output does not receive a new value and therefore holds its previous state.

This experiment reinforced the importance of assigning an output for every possible condition in combinational logic.

⸻

📌 4. RTL Schematic of Incomplete IF-ELSE Statement 🔄

The next experiment focused on an incomplete IF-ELSE based design.

The RTL was synthesized to observe how conditional assignments are interpreted by the synthesis tool.

⚙️ Yosys Commands

yosys

read_verilog incom_if2.v

synth -top incom_if2

show

🖼️ Output


<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 01 14 PM" src="https://github.com/user-attachments/assets/64f5a325-2605-4f4f-ab27-8cdf3a49e783" />

Figure 4: RTL schematic of the incomplete IF-ELSE design.

The Yosys-generated schematic represents the hardware inferred from the incomplete conditional description.

Studying this schematic helped me understand that synthesis tools do not simply reproduce the Verilog code. Instead, they interpret the behavior described by the RTL and generate the hardware necessary to implement that behavior.

⸻

📌 5. Incomplete Case Statement

Case statements are commonly used to describe selection logic such as multiplexers.

When a case statement does not cover all possible select conditions, the output may need to retain its previous value. This can result in latch inference during synthesis.

Verilog Code
module incomp_case (
    input i0,
    input i1,
    input i2,
    input [1:0] sel,
    output reg y
);

always @(*)
begin
    case(sel)
        2'b00 : y = i0;
        2'b01 : y = i1;
    endcase
end

endmodule

Only two select conditions are explicitly defined in this example. The remaining conditions do not contain an output assignment.

⚙️ Simulation

iverilog -o incomp_case incomp_case.v incomp_case_tb.v

./a.out

gtkwave incomp_case.vcd

🖼️ Simulation Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 01 32 PM" src="https://github.com/user-attachments/assets/e737b258-17db-484d-9456-530f85efebe4" />

Figure 5: GTKWave simulation waveform of the incomplete case statement.

The waveform shows the output behavior for different values of the select signal.

For the explicitly defined case values, the output follows the corresponding input. For unspecified cases, the output retains its previous value.

This provides a clear example of how incomplete case statements can introduce latch-like behavior.

⚙️ Yosys Commands

yosys

read_verilog incomp_case.v

synth -top incomp_case

show

🖼️ Synthesized Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 02 02 PM" src="https://github.com/user-attachments/assets/242c2b08-f573-4087-ab0c-13f6cea0c302" />

Figure 6: RTL schematic of the incomplete case design.

The synthesized schematic illustrates the hardware required to preserve the output when no case branch is selected.

The presence of storage behavior demonstrates why complete assignments are important when describing combinational circuits.

This experiment connected the simulation waveform with the actual hardware structure inferred by Yosys.

⸻

📌 6. Complete Case Statement ✅

A complete case statement provides an assignment for every possible input condition.

A default branch is commonly used to handle the remaining conditions and ensure that the output is always assigned.

Verilog Code
module comp_case (
    input i0,
    input i1,
    input i2,
    input [1:0] sel,
    output reg y
);

always @(*)
begin
    case(sel)
        2'b00 : y = i0;
        2'b01 : y = i1;
        default : y = i2;
    endcase
end

endmodule

⚙️ Simulation Commands

iverilog -o comp_case comp_case.v comp_case_tb.v

./a.out

gtkwave comp_case.vcd

🖼️ Simulation Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 02 13 PM" src="https://github.com/user-attachments/assets/f67e0bcc-f221-4da4-ae8e-a7c233edd721" />

Figure 7: Simulation waveform of the complete case statement.

The waveform shows that the output is assigned for all possible select conditions.

Unlike the incomplete case experiment, there is no requirement for the output to remember its previous value. The design therefore represents predictable combinational behavior.

This comparison helped me understand why complete case statements are preferred for combinational RTL.

⚙️ Yosys Commands

yosys

read_verilog comp_case.v

synth -top comp_case

show

🖼️ Synthesized Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 02 24 PM" src="https://github.com/user-attachments/assets/235ff188-4581-437a-a506-2cac9ba1a36c" />

Figure 8: RTL schematic of the complete case statement.

The generated schematic shows a combinational implementation of the complete case statement.

Since all possible select conditions are covered, unnecessary storage behavior is avoided.

Comparing this schematic with the incomplete case schematic demonstrates how RTL coding style directly influences the resulting hardware.

⸻

📌 7. Partial Case Assignment

Partial assignment occurs when different outputs are assigned in different case branches, leaving some outputs without assignments under certain conditions.

Verilog Code
module partial_case_assign (
    input i0,
    input i1,
    input i2,
    input [1:0] sel,
    output reg y,
    output reg x
);

always @(*)
begin
    case(sel)
        2'b00 : begin
            y = i0;
            x = i2;
        end

        2'b01 : y = i1;

        default : begin
            x = i1;
        end
    endcase
end

endmodule

The example shows that y and x are not assigned in every branch.

⚙️ Synthesis Commands

yosys

read_verilog partial_case_assign.v

synth -top partial_case_assign

show

🖼️ Synthesized Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 02 33 PM" src="https://github.com/user-attachments/assets/bbe9190f-b59c-4973-b2dd-c3d653408619" />

Figure 9: Synthesized representation of the partial case assignment.

The schematic demonstrates the effect of incomplete assignments to individual outputs.

This experiment was particularly useful because it shows that simply using a case statement does not guarantee purely combinational hardware. Every output must receive an appropriate assignment for all possible conditions.

⚙️ Simulation Commands

iverilog -o partial_case partial_case_assign.v partial_case_assign_tb.v

./a.out

gtkwave partial_case.vcd

🖼️ Simulation Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 02 43 PM" src="https://github.com/user-attachments/assets/63677e4a-29ef-4db3-98d1-f15a0fe63f58" />

Figure 10: Simulation waveform of the partial case assignment.

The waveform illustrates how the outputs behave when particular branches do not provide assignments.

An output that is not assigned in the active branch can retain its previous value. This provides a practical demonstration of how incomplete assignments can introduce storage behavior.

The experiment helped me understand the importance of assigning every output in every possible combinational path.

⸻

📌 8. Bad Case Assignment

This experiment was used to study a case-based input selection circuit.

The design has four inputs and a 2-bit select signal. Each possible select combination is associated with one input.

Verilog Code
module bad_case (
    input i0,
    input i1,
    input i2,
    input i3,
    input [1:0] sel,
    output reg y
);

always @(*)
begin
    case(sel)
        2'b00: y = i0;
        2'b01: y = i1;
        2'b10: y = i2;
        2'b11: y = i3;
    endcase
end

endmodule

⚙️ Simulation Commands

iverilog -o bad_case bad_case.v bad_case_tb.v

./a.out

gtkwave bad_case.vcd

🖼️ Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 02 51 PM" src="https://github.com/user-attachments/assets/1f0a2fb7-20ed-42a7-9d28-6afb8fb21775" />

Figure 11: Simulation result of the case-based selection circuit.

The waveform demonstrates the selection of different input signals based on the value of sel.

Although the experiment is referred to as a "bad case" example, the complete four-way selection demonstrates how case statements can be used to describe multiplexing behavior.
⸻

📌 9. Multiplexer (MUX) 🔀

A multiplexer is a combinational circuit that selects one input from multiple available inputs.

The select signals determine which input is transferred to the output.

⚙️ Simulation Commands

iverilog -o mux mux.v mux_tb.v

./a.out

gtkwave mux.vcd

🖼️ Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 03 04 PM" src="https://github.com/user-attachments/assets/3f98b933-3423-4001-b290-0839b40886e0" />

Figure 12: Simulation waveform of the multiplexer.

The waveform shows the output changing according to the selected input.

As the select signals change, a different input is connected to the output. This experiment provided a practical understanding of how selection logic is represented in RTL and verified through simulation.

Multiplexers are fundamental building blocks used extensively in processors, datapaths, control logic, and digital systems.

⸻

📌 10. Demultiplexer (DEMUX) 🔀

A demultiplexer performs the reverse operation of a multiplexer.

It receives a single input and routes that input to one of several output lines according to the select signal.

⚙️ Simulation Commands

iverilog -o demux demux.v demux_tb.v

./a.out

gtkwave demux.vcd

🖼️ Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 03 13 PM" src="https://github.com/user-attachments/assets/75cd44f6-85ab-4614-bdf3-f88308a26491" />

Figure 13: Simulation waveform of the demultiplexer.

The waveform demonstrates how the input signal is routed to different output lines depending on the select signal.

Only the selected output becomes active while the remaining outputs remain inactive.

This experiment helped me understand the reverse operation of multiplexing and how selection logic can be used for signal routing.

⸻

📌 11. Ripple Carry Adder ➕

An 8-bit Ripple Carry Adder is a combinational arithmetic circuit used to perform binary addition.

It consists of multiple full-adder stages connected in sequence. The carry generated from one stage becomes the carry input for the next stage.

Because the carry propagates through the stages, the circuit is called a Ripple Carry Adder.

⚙️ Simulation Commands

iverilog -o ripple_carry_adder ripple_carry_adder.v ripple_carry_adder_tb.v

./a.out

gtkwave ripple_carry_adder.vcd

🖼️ Output

<img width="958" height="930" alt="WhatsApp Image 2026-08-22 at 2 03 22 PM" src="https://github.com/user-attachments/assets/b90530d5-117a-4e3b-b038-b7d53d99beb3" />

Figure 14: Simulation output of the 8-bit Ripple Carry Adder.

The waveform shows the sum and carry outputs for different combinations of input values.

The experiment demonstrates how binary arithmetic can be constructed using smaller combinational building blocks.

The carry propagation between the adder stages is an important characteristic of the Ripple Carry Adder. Although the architecture is simple, the propagation delay increases as the number of bits increases.

⸻

**🔄 Overall Synthesis & Verification Flow**

The experiments followed a common RTL design and verification methodology:

        Verilog RTL
             ↓
       RTL Simulation
             ↓
        Icarus Verilog
             ↓
        GTKWave Analysis
             ↓
       RTL Synthesis
             ↓
           Yosys
             ↓
    RTL Schematic Generation
             ↓
   Logic Optimization & Analysis
             ↓
      Hardware Understanding

This flow helped me understand the complete relationship between RTL code, simulation behavior, synthesized hardware, and optimization.

**💡 Key Learnings**

Through this module, I gained practical understanding of several important RTL design concepts:

🔹 Incomplete assignments can lead to latch inference.

🔹 Every output of combinational logic should be assigned for all possible conditions.

🔹 Complete case statements help create predictable combinational hardware.

🔹 Partial assignments can introduce unintended storage behavior.

🔹 Simulation waveforms are useful for identifying unexpected RTL behavior.

🔹 Yosys provides a visual way to understand the hardware inferred from RTL.

🔹 Multiplexers select one signal from multiple inputs.

🔹 Demultiplexers route one input to one of several outputs.

🔹 Ripple Carry Adders demonstrate how arithmetic circuits can be constructed from full adders.

🔹 RTL coding style has a direct impact on synthesized hardware.

**🏁 Conclusion**

Module 5 provided hands-on experience with RTL optimization, combinational logic design, synthesis, and functional verification.

By working with incomplete IF statements, case statements, partial assignments, multiplexers, demultiplexers, and a Ripple Carry Adder, I was able to understand how Verilog descriptions are interpreted by synthesis tools.

The experiments also highlighted an important principle in RTL design:

Good RTL coding is not only about making the simulation work — it is about describing the hardware you actually want to build.

